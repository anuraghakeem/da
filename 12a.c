#include<stdio.h>
#include<stdlib.h>
typedefstructnode{
structnode*rightchild;
intdata;
structnode*leftchild;
}*treepointer;
treepointerroot;
treepointermodified_search(intn)
{
treepointertemp=root,trail;
if(root==NULL)
returnNULL;
while(temp)
{
trail=temp;
if(n<temp->data)
temp=temp->leftchild;
elseif(n>temp->data)
temp=temp->rightchild;
else
returnNULL;
}
returntrail;
}
voidinsert(intn)
{
treepointertemp,ptr;
temp=modified_search(n);
if(temp||!root)
{
ptr=(treepointer)malloc(sizeof(*ptr));
ptr->data=n;
ptr->rightchild=ptr->leftchild=NULL;
if(root)
{
if(n<temp->data)
temp->leftchild=ptr;
else
temp->rightchild=ptr;
}
else
root=ptr;
}
}
voidsearch(intn)
{
if(root==NULL)
printf("Thetreeisempty\n");
else{
treepointertemp=modified_search(n);
if(!temp)
printf("Enteredelementfoundinthetree\n");
else
printf("Notfound\n");
}

}
voidprint_in_order(treepointertemp)
{
if(temp)
{
print_in_order(temp->leftchild);
printf("%d",temp->data);
print_in_order(temp->rightchild);
}
}
intmain()
{
root=NULL;
intchoice,n;
while(1)
{
printf("1.Insert\n2.search\n3.Inorderdisplay\n");
scanf("%d",&choice);
switch(choice)
{
case1:
printf("Entertheelementtoadd:");
scanf("%d",&n);
insert(n);
break;
case2:
printf("Entertheelementtodelete:");
scanf("%d",&n);
search(n);
break;
case3:
print_in_order(root);
printf("\n");
break;
default:
exit(0);
}
}
}
